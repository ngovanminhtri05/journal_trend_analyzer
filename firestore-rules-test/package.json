{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator-backed unit tests for firestore.rules (admin panel collections).",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-journal --config ../firebase.json \"jest\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "jest": "^29.7.0"
  }
}
