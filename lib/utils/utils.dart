/// Utils layer (MVVM) — pure, framework-free helpers shared across layers.
///
/// Holds small stateless helpers (formatters, constants, pure transforms) that
/// have no Flutter or Firebase dependency. Barrel exports are added as helpers
/// are introduced.
library;
